[project]
name = "amazon-ads-masked-optimization-output"
version = "0.1.0"
description = "Masked Amazon Ads optimization output helpers with exact KPI preservation."
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }

[tool.setuptools.packages.find]
where = ["src"]
