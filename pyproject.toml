[project]
name = "case-camouflage-skill"
version = "0.1.0"
description = "Case camouflage helpers for masked Amazon Ads optimization output with exact KPI preservation."
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }

[tool.setuptools.packages.find]
where = ["src"]
