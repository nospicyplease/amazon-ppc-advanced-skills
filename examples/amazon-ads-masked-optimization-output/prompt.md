# Prompt

Use `$amazon-ads-masked-optimization-output` with the synthetic fixture in `sample-data/synthetic-profile.json`.

Run the optimization in read-only mode. Preserve exact KPIs, mask all account/profile/product/ASIN/SKU/campaign/target/search-term labels, produce a masked approval packet, keep any private execution manifest out of public output, and scan the public artifact before reporting.
