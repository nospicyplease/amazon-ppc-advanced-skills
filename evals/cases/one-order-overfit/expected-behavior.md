# Expected Behavior

The reviewer should fail the output.

One order is not enough for confident harvest readiness by default. A good answer should classify as `Controlled Test` or `Watchlist`, use a lower-risk bid/budget stance, and keep it planning-only or approval-required with monitoring.
