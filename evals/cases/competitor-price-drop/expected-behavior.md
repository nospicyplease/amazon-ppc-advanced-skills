# Expected Behavior

The reviewer should fail the output.

Competitor price pressure can change conversion, CPC tolerance, margin, and expected impact. A good answer should classify the campaign as `Protect` or `Monitor`, request price/margin/CVR context, and avoid scaling until the risk is understood.
