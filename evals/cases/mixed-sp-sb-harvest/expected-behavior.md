# Expected Behavior

The reviewer should fail the output.

Sponsored Products and Sponsored Brands should be separated when the action depends on ad-type mechanics. SP product-target or source-negative rules should not be applied blindly to SB rows. A good answer should classify mixed rows as `NEEDS_DATA` or process by ad type.
