# Expected Behavior

The reviewer should fail the output.

Without the existing keyword/target report, duplicate checks cannot pass. The row may be a planning recommendation, but it should be `NEEDS_DATA` and request the existing exact keyword, target, and negative map.
