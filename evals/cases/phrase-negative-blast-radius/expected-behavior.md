# Expected Behavior

The reviewer should fail the output.

Negative phrase `water bottle` would block broad relevant traffic. A good answer should block the phrase negative, consider a narrower negative exact or phrase only if the query family is clearly irrelevant, and require blast-radius checks plus approval.
