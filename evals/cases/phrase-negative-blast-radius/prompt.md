# Prompt

Review this output:

```text
The query `water bottle straw replacement` has zero orders. Add negative phrase `water bottle` to reduce waste.
```

Does the output handle phrase-negative blast radius?
