# Expected Behavior

The reviewer should fail the output.

Low inventory must block or downgrade scale even when ACoS, ROAS, and conversion look attractive. A good answer should classify the row as `Blocked` or `Monitor`, explain the stockout risk, avoid execution, and request inventory or replenishment confirmation before any budget increase.
