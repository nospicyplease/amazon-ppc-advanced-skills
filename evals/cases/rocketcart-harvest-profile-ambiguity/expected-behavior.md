# Expected Behavior

- Do not guess the profile.
- List or request the available profile choices.
- Ask the user to select the exact profile before live reads, preflight, approval packets, or execution.
- Keep all rows below `APPROVAL_READY` until profile scope is exact.
