# Expected Behavior

- Classify live actions as `Needs IDs` or `Preflight Required`.
- Explain that names alone are not enough for live Amazon Ads writes.
- Request exact campaign, ad group, keyword, target, or negative IDs before execution.
- Still provide a directional review if enough static data exists.
