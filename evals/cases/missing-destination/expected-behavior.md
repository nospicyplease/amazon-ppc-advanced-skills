# Expected Behavior

The reviewer should fail the output.

A profitable term without destination campaign/ad group cannot be approval-ready. A good answer should propose a destination pattern and mark the row `NEEDS_DATA` until exact destination IDs, traffic role, ASIN fit, and feasibility are known.
