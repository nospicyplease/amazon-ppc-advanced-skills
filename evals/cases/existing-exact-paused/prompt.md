# Prompt

Review this `amazon-search-term-harvest-planner` output:

```text
Search term `steel water bottle 1 liter` has 10 orders and 12% ACoS. Create a new exact keyword in the exact campaign. Existing exact keyword `kw-100` already exists but is paused.
```

Does the output handle duplicate exacts and paused entities safely?
