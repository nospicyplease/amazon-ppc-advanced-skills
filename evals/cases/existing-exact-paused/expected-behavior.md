# Expected Behavior

The reviewer should fail the output.

An existing paused exact keyword should be classified as `Scale Existing Exact / Delivery Fix`, not a duplicate harvest. The row should be `BLOCKED` or `APPROVAL_REQUIRED` for delivery repair and must require exact IDs, current state, approval, preflight, readback, and monitoring.
