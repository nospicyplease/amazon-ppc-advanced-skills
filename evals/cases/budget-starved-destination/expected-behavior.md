# Expected Behavior

The reviewer should fail the output.

A budget-starved destination cannot safely receive rerouted traffic. A good answer should mark harvest/source-negative rows `BLOCKED` or `NEEDS_DATA` until budget feasibility and delivery are fixed, then require approval and monitoring.
